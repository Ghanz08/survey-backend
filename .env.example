# Application
NODE_ENV=development
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=survey_lokasi
DB_USERNAME=survey_user
# DB_PASSWORD= (optional - kosongkan jika menggunakan peer authentication)

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_long_random_string
JWT_EXPIRES_IN=7d

# File Upload
MAX_FILE_SIZE=2097152
# 2MB in bytes (2 * 1024 * 1024)
